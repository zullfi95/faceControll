# Способы регистрации пользователей

## 📋 Обзор

В системе доступны несколько способов регистрации пользователей. Ниже описаны все варианты, их статус и рекомендации по использованию.

---

## ✅ Способ 1: Регистрация через интерфейс (РАБОТАЕТ)

### Описание
Самый надежный и универсальный способ регистрации. Пользователь создается через веб-интерфейс с загрузкой фото, затем синхронизируется с терминалом.

### Как работает:
1. **Открытие формы регистрации**
   - Нажать кнопку "Добавить сотрудника"

2. **Заполнение данных**
   - Ввести Hikvision ID (например, "1001") - только ASCII символы `a-zA-Z0-9_-`, максимум 32 символа
   - Ввести ФИО
   - Указать отдел (опционально)

3. **Загрузка фото**
   - Вариант A: Загрузить файл с компьютера (JPEG, PNG, до 5 MB)
   - Вариант B: Сделать фото через камеру браузера
   - Фото автоматически сжимается до 200 KB

4. **Создание пользователя**
   - Система создает запись в базе данных
   - Фото сохраняется на сервере

5. **Синхронизация с терминалом**
   - Нажать кнопку "Синхронизировать"
   - Система создает пользователя на терминале через ISAPI JSON API
   - Добавляет фото через FDLib API
   - Процесс занимает 3-7 секунд

### Преимущества:
- ✅ Работает на всех версиях прошивки
- ✅ Можно использовать готовые фото
- ✅ Полный контроль процесса
- ✅ Можно редактировать данные перед синхронизацией

### Недостатки:
- ⚠️ Требует наличие фото заранее
- ⚠️ Нужна синхронизация с терминалом (может занять время)

### API Endpoints:
- `POST /api/users/` - создание пользователя
- `POST /api/users/{user_id}/upload-photo` - загрузка фото
- `POST /api/users/{user_id}/sync-to-device` - синхронизация с терминалом

### Технические детали:
- Используется двухэтапный процесс: создание пользователя (JSON) + добавление фото (FDLib JSON API)
- Подробнее см. [TECHNICAL_DETAILS.md](TECHNICAL_DETAILS.md)

### Статус: ✅ **РАБОТАЕТ**

---

## ❌ Способ 2: Remote Registration (НЕ РАБОТАЕТ на V4.38.0)

### Описание
Удаленный запуск регистрации на терминале. Пользователь подходит к терминалу и регистрируется напрямую, система автоматически получает данные.

### Как работает:
1. **Запуск режима регистрации**
   - Нажать "Добавить сотрудника" → "Регистрация с устройства"
   - Ввести ID, ФИО, время ожидания (60 сек)
   - Система отправляет команду на терминал через ISAPI

2. **Регистрация на терминале**
   - Терминал переходит в режим ожидания регистрации
   - Пользователь подходит к терминалу
   - Сканирует лицо в течение указанного времени

3. **Автоматическое завершение**
   - Система проверяет статус регистрации (polling)
   - Получает фото с терминала
   - Создает пользователя в БД
   - Помечает как синхронизированного

### Преимущества:
- ✅ Пользователь регистрируется напрямую на терминале
- ✅ Автоматическое получение фото
- ✅ Не нужно загружать фото заранее

### Недостатки:
- ❌ **НЕ РАБОТАЕТ на прошивке V4.38.0**
- ❌ Терминал возвращает `notSupport` при попытке запуска
- ❌ Требует обновления прошивки до версии, поддерживающей Remote Registration

### API Endpoints:
- `POST /api/devices/start-remote-enrollment` - запуск регистрации
- `GET /api/devices/check-enrollment-status/{hikvision_id}` - проверка статуса
- `POST /api/devices/complete-remote-enrollment` - завершение регистрации

### Статус: ❌ **НЕ РАБОТАЕТ** (требуется обновление прошивки)

### Решение:
Обновить прошивку терминала DS-K1T343EFWX до версии, поддерживающей Remote Registration (обычно V4.50+ или V5.0+).

---

## 🔄 Способ 3: Ручная регистрация на терминале + импорт (НЕ РЕАЛИЗОВАНО)

### Описание
Регистрация пользователей напрямую на терминале, затем экспорт данных и импорт в систему.

### Как работает:
1. **Регистрация на терминале**
   - Администратор регистрирует пользователей через интерфейс терминала
   - Данные сохраняются на терминале

2. **Экспорт данных**
   - Система запрашивает список пользователей через ISAPI
   - Получает фото и данные каждого пользователя

3. **Импорт в систему**
   - Создание пользователей в БД
   - Сохранение фото на сервере
   - Синхронизация статуса

### Преимущества:
- ✅ Можно регистрировать пользователей без веб-интерфейса
- ✅ Полезно для массовой регистрации

### Недостатки:
- ⚠️ Требует реализации экспорта/импорта
- ⚠️ Может быть сложнее для пользователей

### Статус: ⚠️ **НЕ РЕАЛИЗОВАНО** (можно добавить)

---

## 📊 Сравнительная таблица

| Способ | Статус | Прошивка | Сложность | Рекомендация |
|--------|--------|----------|-----------|--------------|
| **Через интерфейс** | ✅ Работает | Любая | Низкая | **Использовать** |
| **Remote Registration** | ❌ Не работает | V4.50+ | Средняя | Обновить прошивку |
| **Ручная + импорт** | ⚠️ Не реализовано | Любая | Высокая | Можно добавить |

---

## 🎯 Рекомендации

### Для текущей прошивки (V4.38.0):
**Используйте Способ 1 (Регистрация через интерфейс)**

Это единственный рабочий способ на текущей прошивке. Он надежен и прост в использовании.

### После обновления прошивки:
**Можно использовать Способ 2 (Remote Registration)**

После обновления прошивки до версии, поддерживающей Remote Registration, этот способ станет доступен автоматически (код уже реализован).

---

## 🔧 Технические детали

### Текущая реализация:
- ✅ Полная поддержка регистрации через интерфейс
- ✅ Синхронизация с терминалом через ISAPI
- ✅ Загрузка и сохранение фото
- ✅ Обработка ошибок и валидация

### Что нужно для Remote Registration:
- Обновление прошивки терминала
- Проверка поддержки endpoint `/ISAPI/AccessControl/RemoteControl/register`
- Тестирование после обновления

### Что можно добавить:
- Экспорт/импорт пользователей с терминала
- Массовая регистрация
- Интеграция с другими системами

---

## 📝 Примеры использования

### Регистрация через интерфейс (работает сейчас):

```javascript
// Frontend
1. Открыть форму регистрации
2. Заполнить данные: ID, ФИО, отдел
3. Загрузить фото (файл или камера)
4. Нажать "Создать"
5. Система автоматически синхронизирует с терминалом
```

### Remote Registration (после обновления прошивки):

```javascript
// Frontend
1. Выбрать "Регистрация с устройства"
2. Ввести ID, ФИО, время ожидания
3. Нажать "Запустить регистрацию"
4. Пользователь подходит к терминалу
5. Система автоматически получает данные
```

---

## 🐛 Известные проблемы

1. **Remote Registration не работает на V4.38.0**
   - Причина: Прошивка не поддерживает endpoint
   - Решение: Обновить прошивку

2. **Синхронизация может занять время**
   - Причина: Загрузка фото на терминал через ISAPI
   - Решение: Это нормально, процесс занимает 5-10 секунд

---

## 📞 Поддержка

Если возникли проблемы с регистрацией:
1. Проверьте подключение к терминалу
2. Убедитесь, что фото загружено
3. Проверьте логи backend
4. Используйте endpoint `/api/devices/{device_id}/status` для диагностики




