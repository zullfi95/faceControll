# Руководство пользователя

Инструкция по работе с веб-интерфейсом системы Face Access Control.

## Вход в систему

Откройте браузер и перейдите по адресу вашего сервера:
- **Production:** http://46.62.223.55
- **Локально:** http://localhost

> Система не требует аутентификации в базовой версии. Для добавления авторизации см. документацию по безопасности.

---

## Навигация

Главное меню содержит три раздела:

- **👥 Сотрудники** - управление базой сотрудников
- **📊 Отчеты** - просмотр отчетов о посещаемости
- **⚙️ Настройки** - конфигурация устройств

---

## Раздел: Сотрудники

### Добавление нового сотрудника

1. Нажмите кнопку **"Добавить сотрудника"**

2. Заполните форму:
   - **ID (с терминала):** Уникальный номер сотрудника (например, `1001`)
     - Только ASCII символы: `a-zA-Z0-9_-`
     - Максимум 32 символа
     - Этот ID должен совпадать с Employee No на терминале
   - **ФИО:** Полное имя сотрудника
   - **Отдел:** Название отдела (необязательно)

3. Нажмите **"Сохранить"**

> **Примечание:** На этом этапе создается только запись в базе данных. Для работы с терминалом нужно загрузить фото и синхронизировать.

### Загрузка фото лица

1. Найдите сотрудника в списке
2. Нажмите кнопку **"Фото"**
3. В открывшемся окне:
   - Перетащите фото в область загрузки
   - Или нажмите на область и выберите файл
4. Убедитесь, что preview отображается корректно
5. Нажмите **"Сохранить"**

**Требования к фото:**
- ✅ Формат: JPG, PNG
- ✅ Размер: до 5 MB (автоматически сжимается до 200 KB)
- ✅ Лицо анфас (фронтальный ракурс)
- ✅ Хорошее освещение
- ✅ Нейтральное выражение лица
- ✅ Без очков, головных уборов (желательно)

> **Примечание:** Фото автоматически сжимается до 200 KB для оптимизации передачи на терминал.

### Синхронизация с терминалом

После загрузки фото:

1. Нажмите кнопку **"Синхронизировать"** рядом с именем сотрудника
2. Система автоматически:
   - Проверит наличие активного устройства
   - Создаст пользователя на терминале через ISAPI JSON API
   - Добавит фото через FDLib API
3. Дождитесь сообщения об успешной синхронизации (процесс занимает 3-7 секунд)

После синхронизации:
- ✅ Статус сотрудника изменится на **"Синхронизирован"**
- ✅ Сотрудник сможет проходить через терминал

### Статусы сотрудника

- 🟢 **Активен** - сотрудник работает в компании
- 🔴 **Неактивен** - сотрудник уволен/временно неактивен
- 🔵 **Синхронизирован** - данные переданы на терминал

### Редактирование данных

Для изменения информации о сотруднике:
1. Нажмите на имя сотрудника
2. Отредактируйте поля
3. Сохраните изменения

> **Примечание:** После изменения ФИО или фото повторите синхронизацию.

### Удаление сотрудника

**Пока не реализовано в интерфейсе.**

Для удаления используйте API:
```bash
curl -X DELETE http://46.62.223.55/api/users/{user_id}/remove-from-device
```

---

## Раздел: Отчеты

### Дневной отчет посещаемости

1. Выберите дату в календаре (по умолчанию - сегодня)
2. Система автоматически сформирует отчет

### Информация в отчете

Таблица содержит:
- **Сотрудник** - ФИО
- **Вход** - время первого прохода через терминал в этот день
- **Выход** - время последнего прохода
- **Часы** - количество отработанных часов (выход - вход)
- **Статус:**
  - 🟢 **Присутствовал** - есть вход и выход
  - 🔴 **Прогул** - нет ни одного прохода
  - 🟡 **Ошибка данных** - только один проход (вход без выхода или наоборот)

### Расчет времени

**Формула:** `Часы = Время последнего события - Время первого события`

**Пример:**
- Вход: 09:00
- Выход: 18:00
- Часы: 9.00 ч.

**Ограничения:**
- Не учитываются обеденные перерывы
- Не учитываются промежуточные проходы
- Учитывается только первый и последний проход за день

### Экспорт отчетов

**Пока не реализовано в интерфейсе.**

Для экспорта используйте API:
```bash
curl "http://46.62.223.55/api/reports/daily?date_str=2024-12-04" > report.json
```

---

## Раздел: Настройки

### Первичная настройка устройства

При первом запуске системы необходимо добавить терминал:

1. Перейдите на вкладку **"Настройки"**
2. Нажмите **"Добавить устройство"**
3. Заполните форму:
   - **Название:** Например, "Терминал Вход"
   - **IP адрес:** `10.0.0.100` (IP терминала в VPN сети)
   - **Пользователь:** `admin` или созданный API пользователь
   - **Пароль:** Пароль от терминала
4. Нажмите **"Сохранить"**

> **Важно:** Используйте VPN IP адрес терминала, а не локальный!

### Проверка соединения

После добавления устройства:

1. Нажмите кнопку **"Проверить соединение"**
2. Система выполнит:
   - Ping устройства
   - Запрос информации через ISAPI
3. Результат:
   - ✅ **Устройство доступно** - показывает модель, серийный номер, версию ПО
   - ❌ **Устройство недоступно** - показывает ошибку

### Информация об устройстве

Страница настроек показывает:
- **Название** - произвольное имя
- **IP адрес** - адрес терминала в сети
- **Пользователь** - логин для ISAPI
- **Статус** - активно/неактивно
- **Последняя синхронизация** - время последнего успешного добавления пользователя

---

## Типичные сценарии использования

### Сценарий 1: Прием на работу нового сотрудника

1. HR добавляет сотрудника в систему (Сотрудники → Добавить)
2. HR загружает фото сотрудника
3. HR синхронизирует с терминалом
4. Сотрудник может приходить на работу - терминал его узнает
5. Система автоматически фиксирует время прихода/ухода

### Сценарий 2: Проверка опозданий

1. Откройте раздел "Отчеты"
2. Выберите сегодняшнюю дату
3. Посмотрите время входа каждого сотрудника
4. Сотрудники со временем входа > 09:00 опоздали

### Сценарий 3: Подсчет отработанных часов

1. Откройте раздел "Отчеты"
2. Выберите нужную дату
3. Система автоматически покажет часы каждого сотрудника
4. Экспортируйте данные для расчета зарплаты

### Сценарий 4: Увольнение сотрудника

1. Найдите сотрудника в списке
2. Нажмите "Синхронизировать" → выберите "Удалить с терминала"
3. Измените статус на "Неактивен"
4. Сотрудник больше не сможет пройти через терминал

---

## Частые вопросы (FAQ)

### Q: Сотрудник не может пройти через терминал

**A:** Проверьте:
1. ✅ Сотрудник добавлен в систему
2. ✅ Фото загружено
3. ✅ Синхронизация выполнена успешно
4. ✅ Статус "Синхронизирован"
5. ✅ Устройство доступно (Настройки → Проверить соединение)

### Q: События не приходят в систему

**A:** Проверьте:
1. ✅ VPN туннель работает
2. ✅ HTTP Listening настроен на терминале
3. ✅ Терминал может достучаться до сервера (ping 10.0.0.1)
4. ✅ Backend запущен (логи: `docker-compose logs -f backend`)

### Q: Отчет показывает "Ошибка данных"

**A:** Это означает, что за день зафиксирован только один проход (вход или выход, но не оба).

**Возможные причины:**
- Сотрудник забыл отметиться при выходе
- Сотрудник ушел через другой выход (не через терминал)
- Технический сбой терминала

**Решение:** Вручную добавьте событие или скорректируйте данные.

### Q: Можно ли использовать несколько терминалов?

**A:** Да. Система поддерживает несколько устройств:
1. Настройки → Добавить устройство (для каждого терминала)
2. В настройках `.env` укажите IP терминала на входе:
   ```
   TERMINAL_IN_IP=10.0.0.100  # Терминал на входе
   ```
3. События с других IP будут считаться "выходом"

### Q: Как сделать backup данных?

**A:** Backup базы данных:
```bash
ssh root@46.62.223.55
cd /opt/face-access-control
docker-compose exec db pg_dump -U postgres facerecog > backup.sql
```

Восстановление:
```bash
docker-compose exec -T db psql -U postgres facerecog < backup.sql
```

### Q: Можно ли изменить рабочее время?

**A:** Сейчас система просто считает разницу между первым и последним событием дня.

Для учета обедов и перерывов потребуется доработка логики отчетов.

---

## Получение помощи

### Логи системы

Если что-то работает не так:

```bash
# Подключение к серверу
ssh root@46.62.223.55

# Логи всех сервисов
cd /opt/face-access-control
docker-compose logs -f

# Только backend
docker-compose logs -f backend
```

### Сообщения об ошибках

При возникновении проблем:
1. Сделайте скриншот ошибки
2. Скопируйте текст из логов
3. Создайте Issue в репозитории проекта
4. Опишите шаги для воспроизведения

### Поддержка

Для получения помощи:
- 📧 Email: support@example.com
- 💬 GitHub Issues: [ссылка на репозиторий]
- 📖 Документация: `/docs`

---

## Дополнительные возможности

### API для интеграций

Система предоставляет REST API для интеграции с другими системами:
- Swagger документация: http://46.62.223.55/docs
- ReDoc: http://46.62.223.55/redoc

### Webhook события

Можно настроить отправку событий в другие системы (Slack, Telegram, etc).

Пример: отправка уведомления при опоздании сотрудника.

---

## Безопасность

### Рекомендации

1. ✅ Не храните пароли в открытом виде
2. ✅ Регулярно делайте backup базы данных
3. ✅ Используйте SSL/HTTPS для production
4. ✅ Ограничьте доступ к веб-интерфейсу (VPN, IP whitelist)
5. ✅ Регулярно обновляйте систему

### Права доступа

Рекомендуется создать роли:
- **Администратор** - полный доступ
- **HR** - управление сотрудниками
- **Менеджер** - только просмотр отчетов

> Аутентификация и авторизация не реализованы в базовой версии.

---

**Приятной работы! 🎉**

