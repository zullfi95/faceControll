# Face Access Control System

Система контроля доступа и учета рабочего времени на базе терминалов Hikvision с распознаванием лиц.

## Особенности

- ✅ **Распознавание лиц** через терминалы Hikvision (DS-K1T343EFWX и др.)
- ✅ **Полная регистрация пользователей с фото** - создание пользователя на терминале с захватом фото и привязкой через ISAPI
- ✅ **Успешная аутентификация** - пользователи, созданные через интерфейс, полностью проходят аутентификацию на терминале
- ✅ **Управление пользователями** через веб-интерфейс
- ✅ **Автоматический учет рабочего времени** с дневными отчетами
- ✅ **Удаленное управление терминалом** (добавление пользователей, загрузка фото)
- ✅ **VPN подключение** между облачным сервером и офисной сетью
- ✅ **Production-ready** развертывание на Hetzner через Docker

## Архитектура

```
[Офис]                          [Облако Hetzner]
┌─────────────────────┐        ┌──────────────────────┐
│ Терминал            │        │  Backend (FastAPI)   │
│ DS-K1T343EFWX  ────┼────────┼─► PostgreSQL         │
│ 10.0.0.100 (VPN)    │  VPN   │  10.0.0.1 (VPN)      │
└──────────┬──────────┘        └──────────┬───────────┘
           │                              │
      ┌────┴────┐                    ┌────┴────┐
      │ Keenetic│                    │ Frontend│
      │ Router  │                    │ (React) │
      │ + VPN   │                    └─────────┘
      └─────────┘
```

## Технологический стек

**Backend:**
- FastAPI (Python 3.11)
- PostgreSQL
- SQLAlchemy (async)
- Hikvision ISAPI client

**Frontend:**
- React + Vite
- TailwindCSS
- React Query
- Axios

**Infrastructure:**
- Docker & Docker Compose
- Nginx (reverse proxy)
- WireGuard VPN

## Быстрый старт

### 1. Инициализация проекта

```bash
# Клонирование репозитория
git clone <repo-url>
cd face-access-control

# Генерация ключей и создание .env
bash scripts/init-local.sh
```

### 2. Локальная разработка

```bash
# Запуск всех сервисов
docker-compose up -d

# Проверка логов
docker-compose logs -f

# Открыть в браузере
http://localhost
```

### 3. Развертывание на Hetzner

```bash
# Автоматическое развертывание
bash scripts/deploy.sh

# Или вручную (см. docs/DEPLOYMENT.md)
```

### 4. Настройка VPN

```bash
# Генерация ключей WireGuard
cd wireguard
bash generate-keys.sh

# Применение конфигурации на сервере
# Скопируйте wg0-server.conf на сервер в /etc/wireguard/wg0.conf
# Затем на сервере выполните:
# systemctl enable wg-quick@wg0 && systemctl start wg-quick@wg0

# Импорт wg0-client.conf в Keenetic Router
```

Подробная инструкция: [docs/VPN_SETUP.md](docs/VPN_SETUP.md)

### 5. Настройка терминала

1. Зайдите в веб-интерфейс терминала DS-K1T343EFWX
2. Configuration → Network → HTTP Listening
3. Настройте:
   - **Event Alarm IP:** `10.0.0.1` (IP сервера в VPN)
   - **URL:** `/events/webhook`
   - **Port:** `8000`
   - **Protocol:** HTTP

4. В настройках терминала включите ISAPI

Подробная инструкция: [docs/TERMINAL_SETUP.md](docs/TERMINAL_SETUP.md)

## Использование

### Добавление сотрудника

1. Откройте веб-интерфейс: `http://46.62.223.55` (или ваш IP)
2. Перейдите на страницу "Сотрудники"
3. Нажмите "Добавить сотрудника"
4. Заполните данные (ФИО, ID, отдел)
5. Загрузите фото лица
6. Нажмите "Синхронизировать с терминалом"

### Просмотр отчетов

1. Перейдите на страницу "Отчеты"
2. Выберите дату
3. Система покажет:
   - Время входа/выхода каждого сотрудника
   - Количество отработанных часов
   - Статус (Присутствовал/Прогул)

### Настройка устройства

1. Перейдите на страницу "Настройки"
2. Добавьте устройство:
   - **Название:** Терминал Вход
   - **IP адрес:** 10.0.0.100 (VPN IP терминала)
   - **Логин:** admin
   - **Пароль:** ваш пароль от терминала
3. Проверьте соединение

## Структура проекта

```
face-access-control/
├── backend/              # FastAPI backend
│   ├── app/
│   │   ├── main.py      # API endpoints
│   │   ├── models.py    # SQLAlchemy модели
│   │   ├── schemas.py   # Pydantic схемы
│   │   ├── crud.py      # Database operations
│   │   ├── hikvision_client.py  # Hikvision ISAPI
│   │   └── utils/       # Утилиты (шифрование)
│   ├── Dockerfile
│   └── requirements.txt
├── frontend/            # React frontend
│   ├── src/
│   │   ├── pages/       # Страницы
│   │   ├── components/  # Компоненты
│   │   └── App.jsx
│   ├── Dockerfile
│   └── package.json
├── wireguard/           # VPN конфигурация
│   ├── generate-keys.sh
│   ├── wg0-server.conf.template
│   └── wg0-client.conf.template
├── scripts/             # Скрипты автоматизации
│   ├── init-local.sh
│   └── deploy.sh
├── docs/                # Документация
│   ├── DEPLOYMENT.md
│   ├── VPN_SETUP.md
│   └── TERMINAL_SETUP.md
├── docker-compose.yml
├── env.template
└── README.md
```

## Документация

📚 **[Полный индекс документации](docs/INDEX.md)** - навигация по всей документации

### Основные руководства
- 📖 [Развертывание на сервере](docs/DEPLOYMENT.md)
- 🔐 [Настройка VPN](docs/VPN_SETUP.md)
- 🎛️ [Настройка терминала Hikvision](docs/TERMINAL_SETUP.md)
- 📱 [Руководство пользователя](docs/USER_GUIDE.md)

### Техническая документация
- 🔧 [API Документация](docs/API_DOCUMENTATION.md)
- ⚙️ [Технические детали](docs/TECHNICAL_DETAILS.md)
- 📋 [Способы регистрации](docs/REGISTRATION_METHODS.md)

## API Endpoints

### Пользователи
- `POST /users/` - Создание пользователя
- `GET /users/` - Список пользователей
- `POST /users/{id}/upload-photo` - Загрузка фото
- `POST /users/{id}/sync-to-device` - Синхронизация с терминалом

### События
- `POST /events/webhook` - Вебхук для событий от терминала

### Отчеты
- `GET /reports/daily?date_str=YYYY-MM-DD` - Дневной отчет

### Устройства
- `POST /devices/` - Добавление устройства
- `GET /devices/` - Список устройств
- `GET /devices/{id}/status` - Проверка статуса

Полная документация API: `http://your-server/docs` (Swagger UI)

## Безопасность

- ✅ API ключ для вебхука (защита от несанкционированного доступа)
- ✅ Шифрование паролей устройств в БД
- ✅ VPN туннель для связи с терминалом
- ✅ Rate limiting для защиты от DDoS
- ✅ Security headers в Nginx

## Требования

### Сервер (Hetzner)
- Ubuntu 22.04 или новее
- 2+ CPU cores
- 4GB+ RAM
- 20GB+ SSD
- Docker & Docker Compose
- WireGuard

### Роутер офиса
- Keenetic Runner 4G (KN-2210) или аналог
- Поддержка WireGuard VPN

### Терминал
- Hikvision DS-K1T343EFWX или совместимые модели
- Поддержка HTTP Listening
- ISAPI enabled

## Troubleshooting

### Терминал не отправляет события

1. Проверьте VPN соединение: `ping 10.0.0.1` с роутера
2. Проверьте HTTP Listening настройки на терминале
3. Проверьте логи backend: `docker-compose logs -f backend`
4. Проверьте API ключ в заголовке X-API-Key

### Не могу добавить пользователя на терминал

1. Проверьте IP устройства в настройках (должен быть VPN IP)
2. Проверьте логин/пароль устройства
3. Проверьте, что ISAPI включен на терминале
4. Проверьте статус устройства на странице настроек

### VPN не подключается

1. Проверьте, что WireGuard запущен на сервере: `wg show`
2. Проверьте правильность ключей в конфигурации
3. Проверьте, что порт 51820 открыт на сервере
4. Проверьте конфигурацию на Keenetic

### SSH подключение не работает

Если вы не можете подключиться по SSH с локальной машины, но можете через консоль Hetzner:

**На Windows (локально):**
1. Выполните диагностику: `.\scripts\diagnose-ssh-local.ps1`
2. Это проверит доступность сервера и порта 22

**На сервере (через консоль Hetzner):**

Если скрипт недоступен, выполните команды напрямую:

```bash
# Запустить SSH
systemctl start sshd
systemctl enable sshd

# Разрешить SSH в firewall
ufw allow 22/tcp
ufw reload

# Проверить статус
systemctl status sshd
ss -tuln | grep ":22 "
```

Или если скрипт доступен:
1. Выполните диагностику: `bash scripts/diagnose-ssh.sh`
2. Проверьте firewall в панели Hetzner (порт 22 должен быть открыт)

Подробная инструкция: [docs/SSH_TROUBLESHOOTING.md](docs/SSH_TROUBLESHOOTING.md)

## Лицензия

MIT

## Поддержка

Для вопросов и предложений создавайте Issue в репозитории.

